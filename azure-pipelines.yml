trigger: none

pool: powermanage-pool

stages:
  - stage: Terraforminit

    jobs:
    - job: tintu
      displayName: terraforminit

      steps:
        - task: PowerShell@2
          inputs:
           targetType: 'inline'
           script: |
            terraform --version
            terraform init
